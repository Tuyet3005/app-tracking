# Echo App

Simple Node.js app that accepts text input and prints it on the screen.

Run locally:

```powershell
cd d:\_LapTrinh\app-tracking
npm install
npm start
```

Open http://localhost:3000 in your browser.
